# Anthropic API key for the "Ask G247" concierge serverless function.
# Set this in Vercel (Project Settings → Environment Variables) for production,
# and in a local .env file for `npm run dev`. Never commit the real key.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx
